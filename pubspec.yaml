name: goflow_app
description: GoFlow valve controller — BLE native app for the XIAO nRF52840.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.12
  permission_handler: ^11.3.1
  speech_to_text: ^6.6.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
